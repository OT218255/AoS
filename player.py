class Player:
    def loadSprite(self, direction):
        if direction == "UP":
            # load up sprite
        if direction == "RIGHT":
            # load right sprite
        if direction == "DOWN":
            # load down sprite
        if direction == "LEFT":
            # load left sprite